---

# If you need to provide a sudo password, run:
# ansible-playbook src/setup.yml --ask-become-pass
# Or set up passwordless sudo for your user

- hosts: localhost
  roles:
    - base
    - aur-helper
    - vscode
    - cursor
    - claude-code
    - codex
    - gemini
    - rtk
    - zsh
    - docker
    - mise
    - stow
